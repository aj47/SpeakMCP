# Anthropic API Key (Required)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# Agent Configuration (Optional)

# Maximum cost per audit run (USD)
AUDIT_AGENT_MAX_BUDGET=5

# Maximum conversation turns per run
AUDIT_AGENT_MAX_TURNS=50

# Model to use for audits
# Options: claude-sonnet-4-5 (faster, cheaper), claude-opus-4-5 (more thorough)
AUDIT_AGENT_MODEL=claude-sonnet-4-5

# Scheduling interval (minutes)
AUDIT_INTERVAL_MINUTES=30

# Notification Webhooks (Optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/WEBHOOK/URL
